using UnityEngine;

public class NPC : MonoBehaviour
{
    [SerializeField] protected Dialogue _npcDialogue;

    private void Update()
    {
        if (Input.GetKeyDown(KeyCode.K))
        {

        }
    }

}